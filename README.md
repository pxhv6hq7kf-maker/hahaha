
  # Prototype Structure Document

  This is a code bundle for Prototype Structure Document. The original project is available at https://www.figma.com/design/vR1O5Ladd0YKmPtnEinypj/Prototype-Structure-Document.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  